import photoneoLogo from "assets/Photoneo_logo_horizontal_color.svg";

function NavigationHeader() {
    return (
        <header>
            <img src={photoneoLogo} alt="React Logo" />
        </header>
    );
}

export default NavigationHeader;
